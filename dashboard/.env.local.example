# Copie para dashboard/.env.local — o .gitignore da raiz já ignora .env.local

# Público (vai ao browser). NUNCA coloque segredo com prefixo NEXT_PUBLIC_.
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000
NEXT_PUBLIC_STATION_ID=doca-04

# Servidor apenas. Não aparecem no browser.
SLACK_WEBHOOK_URL=
OPENAI_API_KEY=
